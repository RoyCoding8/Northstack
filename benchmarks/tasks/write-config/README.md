# app

Create settings.json in the repository root with a JSON object having:
- "host": the string "127.0.0.1"
- "port": the number 8080
- "debug": the boolean false
- "features": a list of strings containing at least "metrics"
